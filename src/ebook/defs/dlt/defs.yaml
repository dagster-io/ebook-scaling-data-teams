type: dagster_dlt.DltLoadCollectionComponent

attributes:
  loads:
    - source: .loads.localstack_source
      pipeline: .loads.localstack_pipeline

post_processing:
  assets:
    - target: "*"
      attributes:
        group_name: "ingest"
        tags:
          team_size: "medium"
          owner: "data_engineering"